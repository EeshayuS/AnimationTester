<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="@dimen/width"
    android:height="@dimen/height"
    android:tint="@color/multiply_color"
    android:viewportHeight="@integer/viewport_height"
    android:viewportWidth="@integer/viewport_width">

    <group
        android:name="@string/group_multiply_divide"
        android:pivotX="@integer/viewport_center_x"
        android:pivotY="@integer/viewport_center_y">

        <path
            android:name="@string/multiply_name"
            android:pathData="@string/path_multiply"
            android:strokeColor="@android:color/white"

            android:strokeLineCap="square"
            android:strokeWidth="@integer/stroke_width" />

    </group>


</vector>
