<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:height="@dimen/width"
    android:width="@dimen/height"
    android:viewportHeight="@integer/viewport_height"
    android:viewportWidth="@integer/viewport_width">

    <group
        android:name="@string/group_divide_plus"
        android:pivotX="@integer/viewport_center_x"
        android:pivotY="@integer/viewport_center_y">

        <path
            android:name="@string/divide_name"
            android:pathData="@string/path_divide"
            android:strokeWidth="2"
            android:strokeLineCap="square"
            android:strokeColor="@android:color/white"/>

    </group>

</vector>